# Use SQLite locally si no tiene PostgreSQL instalado:
DATABASE_URL=sqlite:///./incidents.db

# Para PostgreSQL, cambie la URL a algo como:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/incidents_db
